function retrieveQuestions(p1, p2) {
    return p1 * p2;              // The function returns the product of p1 and p2
}

function addQuestion(p1, p2) {
    return p1 * p2;              // The function returns the product of p1 and p2
}

function incrementQuestion(p1, p2) {
    return p1 * p2;              // The function returns the product of p1 and p2
}
